import React from 'react';

type NewComponentsType = {
    students:any
}

export const NewComponents = (props: NewComponentsType) => {
    return (
        <div>
            new component
        </div>
    );
};

